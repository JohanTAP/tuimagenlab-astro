---
const steps = [
  "Escaneo en Centros Tu Imagen",
  "Envío de fotos o archivos HTML para aprobación",
  "Fase de Fresado o Impresión 3D",
  "Maquillaje y/o terminación",
  "Entrega en tu clínica (Talca o Chillán)",
];
---
<div id="workflow" class="py-12 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="lg:text-center">
      <h2 class="text-base text-indigo-600 font-semibold tracking-wide uppercase">Proceso</h2>
      <p class="mt-2 text-3xl leading-8 font-extrabold tracking-tight text-gray-900 sm:text-4xl">
        ¿Cómo trabajamos?
      </p>
    </div>
    <div class="mt-10">
      <ol class="space-y-4">
        {steps.map((step, index) => (
          <li class="flex items-center space-x-3">
            <div class="flex items-center justify-center w-8 h-8 border-2 border-indigo-500 rounded-full">
              <span class="text-indigo-500 font-bold">{index + 1}</span>
            </div>
            <span>{step}</span>
          </li>
        ))}
      </ol>
    </div>
  </div>
</div>
